---
apiVersion: v1
kind: Service
metadata:
  name: {{ .serviceName }}-service
  namespace: {{ .namespace }}
spec:
  ports:
  - name: api
    port: {{ .servicePort }}
    targetPort: {{ .targetPort }}
  selector:
    app: {{ .serviceName }}
  type: NodePort